name: CI

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .

        pip install pytest pytest-cov black flake8
    
    - name: Lint with flake8
      run: |
        # Stop the build if there are Python syntax errors or undefined names
        flake8 lwagents --count --select=E9,F63,F7,F82 --show-source --statistics
        # Exit-zero treats all errors as warnings
        flake8 lwagents --count --exit-zero --max-complexity=10 --max-line-length=88 --statistics
    
    - name: Check code formatting with black
      run: |
        black --check lwagents
    
    # - name: Run tests
    #   run: |
    #     # Run tests if they exist, otherwise just do a basic import test
    #     if [ -d "tests" ] && [ "$(ls -A tests)" ]; then
    #       pytest tests/ -v --cov=lwagents --cov-report=xml
    #     else
    #       echo "No tests found, running basic import test"
    #       python -c "import lwagents; print('✓ Package imports successfully')"
    #       python -c "from lwagents import Agent, Graph, State; print('✓ Main classes import successfully')"
    
    # - name: Upload coverage to Codecov
    #   if: matrix.python-version == '3.11'
    #   uses: codecov/codecov-action@v3
    #   with:
    #     file: ./coverage.xml
    #     fail_ci_if_error: false

